{
  "name": "homedao-hardhat",
  "version": "1.0.0",
  "description": "HomeDAO smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:skale": "hardhat run scripts/deploy.js --network skaleBaseTestnet",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.3.0",
    "ethers": "^5.8.0"
  }
}
